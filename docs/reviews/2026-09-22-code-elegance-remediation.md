# 2026-09-22 结构清理与整改

## 范围与验收原则

- 模块：gui_presentation、gui_application，涉及错误语义时包含 inventory_core。
- 共享瓶颈点：app_gui/tool_bridge.py；其他共享入口仅在确认必要时调整。
- 保持库存业务与界面流程不变；保留数据备份、审计和恢复能力。
- 先按职责清理调用和状态，再删除已迁完消费者的旧入口；不以拆文件数量或净删行数代替质量。
- 每项记录生产代码与测试/文档的增删，完成最小回归后再进入下一项。
- 顶层边界与依赖方向保持不变，内部职责调整更新模块软文档；如需改变公共契约，先记录并更新契约。

## R1. 删除执行依赖兜底与测试专用反向导入

- 严重程度：中；模块：gui_presentation；共享瓶颈点：否。
- 位置：operations_panel_execution.py、operations_panel_results.py、operations_panel_actions.py。
- 问题：必需 use case 缺失时临时创建，缺少 summarize 时返回空结果；局部模块为测试 patch 反向依赖主面板。
- 目标：直接使用已构造依赖；翻译与文件对话框直接导入，测试迁到真实依赖位置。
- 契约文档：无需修改硬契约。
- 最小回归：GUI 计划执行、结果展示、导出相关测试。
- 状态：已完成。
- 修改：删除 4 个翻译转发函数、2 个执行兜底函数、测试专用 Qt 导出；动作名称本地化移至 plan_item_desc，由两个消费者直接使用。
- 统计：生产代码 +102 / -145，净减少 43 行；测试 patch 目标同步迁移。
- 回归：计划流程、计划表格、操作面板 124 passed。另收紧共享测试 fixture 的导入异常处理，仅缺少 PySide6 才允许跳过，其他导入错误直接失败。
- 跨模块风险：无公共业务 API 变化。

## R2. 明确失败边界，删除错误猜测

- 严重程度：中；模块：gui_application、gui_presentation、inventory_core。
- 共享瓶颈点：app_gui/tool_bridge.py。
- 问题：任意写异常被包装成备份失败；排序恢复依赖英文错误子串。
- 目标：备份准备错误在准备阶段分类，其他错误保持真实类别；排序按结构化信息恢复。
- 契约文档：先更新模块错误语义说明，不改变现有层级规则。
- 最小回归：bridge、write adapter、Overview 查询、相关契约测试。
- 状态：已完成。
- 修改：请求备份准备抛专用 RuntimeError 子类；bridge 不吞写入实现异常；排序提供结构化详情，保留原错误码。
- 统计：生产代码 +46 / -26，净增加 20 行，换取明确错误类别，未增加备用执行路径。
- 回归：bridge、adapter、数据视图与架构测试 95 passed + 61 subtests；补充本地化排序恢复测试 1 passed。
- 跨模块风险：非准备阶段的异常现在向上暴露，执行流程负责呈现；错误码与正常写入行为保持不变。

## R3. 收口 Overview 查询状态

- 严重程度：高；模块：gui_presentation；共享瓶颈点：否。
- 问题：排序、列筛选、缓存状态散落在面板，多个模块以字符串读取和修改。
- 目标：一个显式状态对象拥有查询条件；迁移所有消费者并删除旧字段，不增加兼容属性。
- 契约文档：更新展示层内部结构软说明。
- 最小回归：GUI 数据视图、Overview、计划标记及刷新测试。
- 状态：已完成。
- 修改：新增 30 行 OverviewQueryState；删除面板上的 5 个查询/缓存字段，全部消费者迁到状态对象；合并排序修正，去掉缓存版本号。
- 统计：生产代码 +56 / -54，净增加 2 行；旧状态属性零残留。
- 回归：数据视图 64 passed，缓存测试改为验证候选值随筛选更新，不再绑定版本计数实现。
- 跨模块风险：仅 Overview 内部实现，跨面板信号和方法不变。

## R4. 清理已被状态存储替代的镜像与兼容访问

- 严重程度：中；模块：gui_presentation；共享瓶颈点：否。
- 问题：草稿与计划已有 store，仍需核查面板中遗留的镜像和兼容读取入口。
- 目标：追踪全部消费者，只删除能够证明冗余的镜像和别名；真实缓存保留并说明来源。
- 契约文档：仅需更新内部实现说明。
- 最小回归：表格录入、暂存、切换数据集、计划执行。
- 状态：已完成。
- 修改：移除旧草稿字典、9 个表格兼容 helper、store 上两个无消费者的方法和未使用的 layout 参数；所有草稿操作走必需的 store。删除 OperationsPanel.plan_items 兼容属性，测试直接读取 PlanStore。
- 统计：生产代码净减少 241 行；其中表格模块相对 R3 净减少 206 行。
- 回归：草稿 store、数据视图、操作面板与计划表格 166 passed；补充验证磁盘刷新后草稿仍保留、计划未被意外提交，1 passed。此前计划流程等 144 passed。
- 跨模块风险：未改变库存写入逻辑；只删除实际构造路径不可达的旧草稿实现。

## 最终验收

- 全量测试：`.venv/Scripts/python.exe -m pytest -o addopts='' -q` → 2042 passed、184 subtests passed，95.99 秒，无失败或跳过。
- lint / diff 检查：`ruff check .` 与 `git diff --check` 均通过。
- 统计口径：`git diff --numstat`，加上新增的 30 行查询状态文件；生产代码 +230 / -492，净减少 262 行。测试 +164 / -111，净增加 53 行；文档单独记录，不计入代码精简。
- 每项已完成：R1 -43 行、R2 +20 行、R3 +2 行、R4 -241 行。
- 文档回填：本记录及三个模块说明已更新；CLAUDE.md 增加重构交付标准，要求说明职责归属、调用方迁移和旧路径删除。
- 边界与后续：本轮内部接口消费者已迁移，库存备份和审计保留。Overview 的网格/表格渲染仍有面板方法绑定，完整组件解耦与核心调用链审计属于后续工作，不将本轮包装为全仓架构重写。
