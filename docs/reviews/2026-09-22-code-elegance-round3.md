# 2026-09-22 第三轮：Overview 交互与行状态收口

## 范围与基线

- 模块：gui_presentation；本轮不修改共享瓶颈点，不改库存写入规则和对外信号载荷。
- 保留前两轮工作区改动，以第三轮开始时的 Python 文件行数为统计基线；前轮全量 2048 tests、184 subtests 通过。
- 契约：仅内部职责变化，无需改机器可读契约；同步更新展示层软说明。

## R8. 单行编辑重复计算草稿状态

- 严重程度：高；共享瓶颈点：否。
- 位置：table_entry_draft_store.resolve_single_row、table_entry_row_resolver.resolve_entry_row、overview_panel_table._row_with_entry_values。
- 问题：单行编辑绕过已有 resolver，再算 confirmed/locked/slot_state/search_text；存在两份状态规则与隐藏的 _data_columns 载荷。
- 目标：编辑后使用同一批量行解析入口，移除独立单行实现与隐式参数，维持即时编辑反馈。
- 回归：草稿状态转换、清空编辑、与暂存值一致时自动清稿，以及数据视图集成测试。
- 状态：已完成；生产代码净减少 55 行。
- 结果：删除 resolve_single_row、私有行键 helper 和 _data_columns 暗传参数；编辑后经 resolve_rows 统一计算状态。补齐日期输入别名同步，修复清空日期时旧值残留导致无法清稿的问题。
- 验证：草稿 store、resolver 与数据视图 106 passed；新增清空全部字段并刷新不恢复旧值的回归断言。

## R9. 网格预填依赖表格实现

- 严重程度：中；共享瓶颈点：否。
- 位置：overview_panel_table 的三种预填函数、OverviewPanel 绑定、overview_panel_interactions。
- 问题：网格通过主面板绑定调用表格模块的预填实现；前台/后台新增预填重复组装相同载荷和位置文本。
- 目标：预填统一由现有交互控制器负责，两个视图直接调用；保留前台/后台信号与提示语区别，删除旧绑定。
- 回归：网格单选、多选、右键菜单；表格当前/历史记录、多位置暂存项；前后台信号载荷。
- 状态：已完成；生产代码净减少 43 行。
- 结果：新增/取出预填由 OverviewInteractionController 统一负责，删除表格模块的 4 个函数和主面板 3 个绑定；前台/后台新增共用载荷与位置文本生成逻辑。
- 验证：Overview 与数据视图 109 passed；新增双信号隔离、位置去重/排序、字母坐标和单位置载荷回归。

## R10. 盒标签两套提交后处理

- 严重程度：中；共享瓶颈点：否。
- 位置：overview_panel_interactions.on_box_context_menu。
- 问题：设置/清除分支复制桥接调用、刷新、状态提示和事件通知；同一结果语义需维护两次。
- 目标：菜单只决定标签输入和操作名，统一调用已有 bridge 并处理结果；保留取消、不带数据集与失败行为。
- 回归：设置/清除成功与失败、取消输入与缺失路径；核对通知操作名。
- 状态：已完成；生产代码净减少 48 行。
- 结果：菜单分支只确定标签、操作名和成功文案；桥接提交、刷新、失败提示和系统通知只有一套实现。
- 验证：Overview 与盒标签测试 50 passed、7 subtests passed；覆盖设置/清除各自成功/失败，以及菜单取消、输入取消与缺失路径。

## 最终验收

- 全量测试：`pytest -o addopts='' -q`，2049 passed、191 subtests passed（78.30 秒）。
- `ruff check .`、`git diff --check`：通过。
- 调用迁移：旧单行解析、私有行键 helper、表格预填函数和主面板绑定均已删除；运行时与测试无残留调用。
- 文档回填完成；变更仅涉及展示层内部实现，无新增跨模块依赖或共享瓶颈点修改。日期清空行为修正已有 GUI 回归覆盖。
- 生产代码净减少 146 行，测试净增加 56 行；包含所有 Python 文件，不把搬移算作精简。三轮累计净减少 568 行生产代码。
